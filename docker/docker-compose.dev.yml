services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dragons
      POSTGRES_PASSWORD: dragons
      POSTGRES_DB: dragons
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
